[Unit]
Description=Filament Manager (FastAPI)
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/3d-drucker-filement/app
ExecStart=/opt/3d-drucker-filement/app/.venv/bin/uvicorn main:app --host 0.0.0.0 --port 8005
Restart=on-failure
RestartSec=3

[Install]
WantedBy=multi-user.target
